packages:
  - name: openssh-client
    manager:
      name: apt
    installatonTest:
      command: ssh
      expectedStatus: 255

  - name: certbot
    manager:
      name: snap
      installFlags: --classic
    installatonTest:
      command: sudo certbot renew --dry-run
