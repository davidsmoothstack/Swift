packages:
  - name: docker
    manager:
      name: script
      scriptName: install-docker.sh
      params:
        version: 5:20.10.15~3-0~ubuntu-jammy
    postInstallTest:
      command: docker version
